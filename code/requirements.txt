# Message Notification Router
# Python 3.10+ recommended (tested on 3.14)
#
# Core offline path (Stages 1–4 offline media) needs no third-party packages.
# For the optional OpenAI multimodal provider, install:
#   pip install openai>=1.40.0
#
# See code/README.md for environment variable configuration.
